# Enable Emacs Eat terminal emulator integration if available
# EAT (Emulate A Terminal) is a terminal emulator for Emacs
# This provides better integration between bash and Emacs when running in an Eat buffer
[ -n "$EAT_SHELL_INTEGRATION_DIR" ] && source "$EAT_SHELL_INTEGRATION_DIR/bash"
